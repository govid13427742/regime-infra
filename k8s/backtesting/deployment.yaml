apiVersion: apps/v1
kind: Deployment
metadata:
  name: backtesting
  namespace: regime
  labels:
    app: backtesting
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backtesting
  template:
    metadata:
      labels:
        app: backtesting
        app.kubernetes.io/part-of: regime-detection
    spec:
      containers:
        - name: backtesting
          image: regime-backtesting
          ports:
            - containerPort: 8004
          envFrom:
            - configMapRef:
                name: regime-config
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3

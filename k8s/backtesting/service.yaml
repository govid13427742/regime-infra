apiVersion: v1
kind: Service
metadata:
  name: backtesting
  namespace: regime
  labels:
    app: backtesting
spec:
  type: ClusterIP
  selector:
    app: backtesting
  ports:
    - port: 8004
      targetPort: 8004
      protocol: TCP

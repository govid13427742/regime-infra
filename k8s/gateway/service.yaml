apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: regime
  labels:
    app: gateway
spec:
  type: LoadBalancer
  selector:
    app: gateway
  ports:
    - port: 80
      targetPort: 6000
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: regime
  labels:
    app: keycloak
spec:
  type: ClusterIP
  selector:
    app: keycloak
  ports:
    - port: 6080
      targetPort: 8080
      protocol: TCP

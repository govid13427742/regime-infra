apiVersion: v1
kind: Service
metadata:
  name: market-data
  namespace: regime
  labels:
    app: market-data
spec:
  type: ClusterIP
  selector:
    app: market-data
  ports:
    - port: 6001
      targetPort: 6001
      protocol: TCP

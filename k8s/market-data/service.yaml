apiVersion: v1
kind: Service
metadata:
  name: market-data
  namespace: regime
  labels:
    app: market-data
spec:
  type: ClusterIP
  selector:
    app: market-data
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP

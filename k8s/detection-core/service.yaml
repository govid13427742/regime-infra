apiVersion: v1
kind: Service
metadata:
  name: detection-core
  namespace: regime
  labels:
    app: detection-core
spec:
  type: ClusterIP
  selector:
    app: detection-core
  ports:
    - port: 6003
      targetPort: 6003
      protocol: TCP

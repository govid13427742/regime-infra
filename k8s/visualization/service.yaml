apiVersion: v1
kind: Service
metadata:
  name: visualization
  namespace: regime
  labels:
    app: visualization
spec:
  type: ClusterIP
  selector:
    app: visualization
  ports:
    - port: 6005
      targetPort: 6005
      protocol: TCP

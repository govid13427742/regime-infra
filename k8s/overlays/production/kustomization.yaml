apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../

# Override image names with ECR registry in production
# Run: kustomize edit set image regime-gateway=${ECR_REGISTRY}/regime-platform:latest
